Get all Recipies for Building Pipelines from frontend object, script_params property contains input of recipe
ns_automation.entities.filter(e => e.class === 'RecipeEntity')

All Model Objects, containing history property with params property having the input values
ns_automation.entities.filter(e => e.class === 'ModelEntity') For History